{
  "name": "@axiom/pos",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc --noEmit",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "decimal.js": "latest",
    "idb": "latest",
    "socket.io-client": "latest"
  },
  "devDependencies": {
    "typescript": "^6.0.0",
    "vitest": "latest"
  }
}
